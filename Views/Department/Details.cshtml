@model Department

<div class="card" style="width:350px">

    <div class="card-body">
        <h4 class="card-title">@Model.Name</h4>
        <p class="card-text">Manger Name: @Model.MangerName</p>
        <a href="/Department/Index" class="btn btn-primary">Return To List</a>

    </div>
</div>